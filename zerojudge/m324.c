// m324. 小賴的期中作業(11的最大整數)

#include <stdio.h>
#include <stdlib.h>



int main() {
    int n;
    scanf("%d", &n);
    int input[n];
    for (int i = 0; i < n; i++) {
        scanf("%d", &input[i]);
    }

    return 0;
}