// 小馬的減肥計畫

#include <stdio.h>
#include <stdlib.h>

#define MAX(i, j) = (i > j ? i : j);

int main() {
  int n, d, max = 0;
  scanf("%d %d\n", &n, &d);
  int hill[n];
  for (int i = 0; i < n; i++) {
    scanf("%d", &hill[i]);
  }
  
  return 0;
}